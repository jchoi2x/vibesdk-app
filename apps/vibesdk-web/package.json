{
	"name": "vibesdk-web",
	"private": true,
	"version": "1.5.0",
	"type": "module",
	"dependencies": {
		"@jchoi2x/types": "workspace:*"
	},
	"scripts": {
		"dev": "DEV_MODE=true vite",
		"build": "tsc -b --incremental && vite build",
		"typecheck": "tsc -b --incremental --noEmit",
		"lint": "eslint .",
		"preview": "vite preview",
		"deploy": "pnpm run build && wrangler deploy",
		"cf-typegen": "wrangler types"
	}
}
