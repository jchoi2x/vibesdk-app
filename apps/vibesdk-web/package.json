{
	"name": "vibesdk-web",
	"private": true,
	"version": "1.5.0",
	"type": "module",
	"dependencies": {
		"@jchoi2x/types": "workspace:*"
	},
	"scripts": {
		"dev": "vite",
		"build": "tsc -b --incremental && vite build && wrangler pages functions build --outdir=\"./dist/_worker.js/\" && cp .assetsignore dist/.assetsignore",
		"typecheck": "tsc -b --incremental --noEmit",
		"lint": "eslint .",
		"preview": "vite preview",
		"deploy": "pnpm run build && wrangler deploy",
		"cf-typegen": "wrangler types"
	}
}
